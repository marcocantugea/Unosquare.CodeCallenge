import { ProductFormValidators } from './product-form-validators';

describe('ProductFormValidators', () => {
  it('should create an instance', () => {
    expect(new ProductFormValidators()).toBeTruthy();
  });
});
